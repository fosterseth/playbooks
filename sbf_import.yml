---
- hosts: localhost
  gather_facts: no

  tasks:
    - import_role:
        name: sbf_debug
        tasks_from: debug.yml
      vars:
        debugmsg: baz
    - import_role:
        name: sbf_debug
        tasks_from: debug.yml
      vars:
        debugmsg: qux

    # ---
    # #roles/foo/tasks/some.yml
    # - debug:
    #     var: bar
