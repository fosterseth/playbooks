- name: Vault lookup
  hosts: 127.0.0.1
  connection: local
  tasks:
    - name: Return all secrets from a path
      debug:
        msg: "{{ lookup('hashi_vault', 'secret=kv/sbf:username token={{ token_var }} url=http://127.0.0.1:8200')}}"
